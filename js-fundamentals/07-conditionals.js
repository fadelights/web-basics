/* the  general syntax for using the
`if` block is shown below */
if (true) {
    /* body */
} else if (true) {
    /* body */
} else {
    /* body */
}

/* another common operator
for checking multiple conditions
is the ternary operator `?` */
let age = 18;
let allowed = age >= 18 ? 'yes' : 'no';
console.log(allowed);
